# 🎓 نظام إدارة طلبات الطلاب الجامعية

نظام ويب متكامل لإدارة طلبات الطلاب في الجامعة، يتيح للطلاب تقديم طلبات متنوعة ومتابعة حالتها، مع لوحة تحكم للإدارة لمعالجة الطلبات.

## 📋 وصف المشروع

هذا النظام يوفر منصة إلكترونية شاملة لتسهيل التواصل بين الطلاب وإدارة الجامعة، حيث يمكن للطلاب:
- تقديم طلبات الإجازات (مرضية، وفاة، وغيرها)
- طلب تخفيض الأقساط الدراسية
- تقديم طلبات التحويل
- متابعة حالة الطلبات المقدمة

## ✨ المميزات

### للطلاب:
- 🔐 تسجيل دخول آمن بالرقم الجامعي والاسم
- 📱 التحقق برمز OTP عبر WhatsApp
- 📝 تقديم طلبات متنوعة (إجازات، تخفيض قسط، تحويل)
- 📎 رفع المستندات والمرفقات
- 📊 متابعة حالة الطلبات المقدمة

### للإدارة:
- 📈 لوحة تحكم شاملة مع إحصائيات
- 📋 عرض جميع الطلبات المقدمة
- ✅ الموافقة أو الرفض على الطلبات
- 🔔 نظام إشعارات للطلبات الجديدة
- 📄 إنشاء تقارير PDF

## 🛠️ التقنيات المستخدمة

| التقنية | الاستخدام |
|---------|-----------|
| PHP | لغة البرمجة الخلفية |
| MySQL | قاعدة البيانات |
| HTML5 | هيكل الصفحات |
| CSS3 | التنسيق والتصميم |
| JavaScript | التفاعلية |
| PDO/MySQLi | الاتصال بقاعدة البيانات |

## 📁 هيكل المشروع

```
gam3a/
├── 📄 login.php              # صفحة تسجيل دخول الطلاب
├── 📄 dashboard.php          # لوحة تحكم الطالب
├── 📄 agaza.php              # معالجة طلبات الإجازة
├── 📄 agaza.html             # نموذج طلب الإجازة
├── 📄 tchfed.html            # نموذج طلب تخفيض القسط
├── 📄 submit_request.php     # معالجة طلب تخفيض القسط
├── 📄 requests.php           # معالجة الطلبات العامة
├── 📄 TLPAT.php              # عرض طلبات الطالب
├── 📄 details.php            # تفاصيل الطلب
├── 📄 db_connection.php      # ملف الاتصال بقاعدة البيانات
├── 📄 check_notifications.php # فحص الإشعارات
├── 📂 admin/                 # مجلد لوحة تحكم الإدارة
│   ├── 📄 admin.html         # صفحة تسجيل دخول المدير
│   ├── 📄 dashboard.php      # لوحة تحكم الإدارة
│   ├── 📄 orders.php         # إدارة الطلبات
│   ├── 📄 update_status.php  # تحديث حالة الطلبات
│   ├── 📄 verify.php         # التحقق من المدير
│   ├── 📄 create_tayid_pdf.php # إنشاء ملفات PDF
│   ├── 📄 cb.css             # ملف التنسيق
│   └── 📄 cb.js              # ملف JavaScript
└── 📂 uploads/               # مجلد رفع الملفات
```

## ⚙️ متطلبات التشغيل

- **خادم ويب:** Apache (XAMPP, WAMP, LAMP)
- **PHP:** الإصدار 7.4 أو أحدث
- **MySQL:** الإصدار 5.7 أو أحدث
- **متصفح ويب حديث**

## 🚀 التثبيت والإعداد

### 1. تثبيت الخادم المحلي
قم بتثبيت [XAMPP](https://www.apachefriends.org/) أو أي خادم محلي آخر.

### 2. نسخ الملفات
```bash
# انسخ مجلد المشروع إلى مجلد htdocs
cp -r gam3a /xampp/htdocs/
```

### 3. إنشاء قاعدة البيانات
1. افتح phpMyAdmin عبر `http://localhost/phpmyadmin`
2. أنشئ قاعدة بيانات جديدة باسم `leave_requests`
3. قم بإنشاء الجداول التالية:

```sql
-- جدول الطلاب
CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_id VARCHAR(20) UNIQUE NOT NULL,
    student_name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول الإجازات
CREATE TABLE leaves (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_name VARCHAR(100) NOT NULL,
    student_number VARCHAR(20) NOT NULL,
    leave_type VARCHAR(50) NOT NULL,
    hospital VARCHAR(100),
    relation VARCHAR(50),
    medical_file LONGBLOB,
    death_proof LONGBLOB,
    request_type VARCHAR(50) DEFAULT 'اجازة',
    status VARCHAR(50) DEFAULT 'في الانتظار',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول طلبات تخفيض القسط
CREATE TABLE tchfed (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_name VARCHAR(100) NOT NULL,
    student_number VARCHAR(20) NOT NULL,
    reason TEXT,
    request_type VARCHAR(50) DEFAULT 'طلب تخفيض القسط',
    status VARCHAR(50) DEFAULT 'في الانتظار',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول طلبات التحويل
CREATE TABLE student_transfers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    student_number VARCHAR(20) NOT NULL,
    request_type VARCHAR(50),
    status VARCHAR(50) DEFAULT 'في الانتظار',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول الطلبات العامة
CREATE TABLE student_requests (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_name VARCHAR(100) NOT NULL,
    student_number VARCHAR(20) NOT NULL,
    request_type VARCHAR(50),
    reason TEXT,
    status VARCHAR(50) DEFAULT 'في الانتظار',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4. تعديل إعدادات قاعدة البيانات
قم بتعديل ملف `db_connection.php` وفقًا لإعداداتك:

```php
$servername = "localhost";
$username = "root";
$password = "";
$database = "leave_requests";
```

### 5. تشغيل المشروع
افتح المتصفح وانتقل إلى:
- **واجهة الطلاب:** `http://localhost/gam3a/login.php`
- **لوحة تحكم الإدارة:** `http://localhost/gam3a/admin/admin.html`

## 📱 خدمة WhatsApp OTP

يستخدم النظام خدمة [ArriveWhats](https://app.arrivewhats.com) لإرسال رموز التحقق. قم بتحديث بيانات الاعتماد في `dashboard.php`:

```php
$instance_id = "YOUR_INSTANCE_ID";
$access_token = "YOUR_ACCESS_TOKEN";
```

## 📸 لقطات الشاشة

> يمكنك إضافة لقطات شاشة للنظام هنا

## 🔒 الأمان

- استخدام PDO/MySQLi مع Prepared Statements لمنع SQL Injection
- تنظيف المدخلات باستخدام `htmlspecialchars()`
- التحقق من الجلسات للصفحات المحمية
- رمز OTP للتحقق من هوية الطالب

## 🤝 المساهمة

نرحب بمساهماتكم! يمكنك:
1. عمل Fork للمشروع
2. إنشاء فرع جديد (`git checkout -b feature/AmazingFeature`)
3. عمل Commit للتغييرات (`git commit -m 'Add some AmazingFeature'`)
4. رفع الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📝 الرخصة

هذا المشروع مرخص بموجب رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## 📞 التواصل

للاستفسارات والدعم الفني، يرجى فتح Issue في المستودع.

---

<div align="center">
  <p>صُنع بـ ❤️ لخدمة الطلاب</p>
</div>
